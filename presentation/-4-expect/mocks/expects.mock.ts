export function processString(param: string) {
  return param;
}

export function processNumber(param: number) {
  return param;
}

export function processObject(param: Record<string, any>) {
  return param;
}

export function processArray(param: any[]) {
  return param;
}
